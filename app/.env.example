# Prover's public evidence lookup and read-only preflight use public CC3 services.
# Do not place private keys, Pinata credentials, or third-party API keys in this frontend.
#
# Optional when a hosting environment needs an explicit canonical URL:
# VITE_APP_URL="https://your-prover-site.example"
